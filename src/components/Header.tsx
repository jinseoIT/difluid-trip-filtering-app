import React from 'react';
import Title from './Title';

const Header = () => {
  return (
    <header className="px-20 pt-30 pb-[34px] pt-[120px]">
      <Title content={`Snap photos and share like \nnever before`} />
    </header>
  );
};

export default Header;
